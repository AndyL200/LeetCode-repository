def convert(s, numRows):
    """
    :type s: str
    :type numRows: int
    :rtype: str
    """
    res = [[]]
    for i in range(len(s)):
        for j in range(numRows):
            res[i].append[s[j]]
            print(s[j])
        print("\n")
    

convert("playwright",4)